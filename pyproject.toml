[project]
name = "hello-agent"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "furqan5911", email = "furqanmalick2001@gmail.com" }
]
requires-python = ">=3.12"
dependencies = []

[project.scripts]
run-agent = "hello_agent.main:my_first_agent"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
